# JobRollo searches the PUBLIC job-board API of each company listed here.
# Format:  <ats> <company-slug>   (one per line; lines starting with '#' are comments)
#
# Find a company's slug in its careers URL:
#   job-boards.greenhouse.io/<slug>   ->  greenhouse <slug>
#   jobs.lever.co/<slug>              ->  lever <slug>
#   jobs.ashbyhq.com/<slug>           ->  ashby <slug>
#
# The three lines below are PLACEHOLDERS to show the format — replace them with
# the real slugs of companies you actually want to apply to.
greenhouse acme-corp
lever globex
ashby initech
